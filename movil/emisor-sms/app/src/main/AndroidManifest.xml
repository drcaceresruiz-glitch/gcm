<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Los permisos, uno a uno y por que:

      INTERNET             - preguntar a GCM si hay algo que mandar.
      SEND_SMS             - lo unico que hace la app. Es el permiso que
                             impide publicarla en Play, y el motivo de que se
                             instale de lado.
      FOREGROUND_SERVICE   - Android mata lo que corre en segundo plano. La
        + DATA_SYNC          unica forma de seguir vivo es un servicio en
                             primer plano, con su notificacion permanente.
      POST_NOTIFICATIONS   - desde Android 13 hay que pedirla para poder
                             ensenar esa notificacion.
      RECEIVE_BOOT_COMPLETED - que vuelva solo cuando el telefono se reinicia,
                             que en obra pasa mas de lo que parece.
      REQUEST_IGNORE_BATTERY_OPTIMIZATIONS - para poder pedir la exencion sin
                             que el usuario tenga que bucear en Ajustes.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Un telefono sin radio para SMS no sirve de emisor. -->
    <uses-feature android:name="android.hardware.telephony" android:required="true" />

    <application
        android:label="@string/app_name"
        android:icon="@android:drawable/ic_dialog_email"
        android:allowBackup="false"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".ServicioEmisor"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".ReceptorArranque"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
